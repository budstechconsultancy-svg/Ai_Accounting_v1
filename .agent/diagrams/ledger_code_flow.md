# Ledger Code Assignment - Visual Flow Diagram

## 📊 Code Assignment Algorithm Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                   NEW LEDGER CREATION REQUEST                    │
│                  (name, hierarchy fields, etc.)                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────────┐
                    │  Start Code        │
                    │  Generation        │
                    └─────────┬──────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────────┐
        │  DECISION 1: Has parent_ledger_id?          │
        └─────┬─────────────────────────────┬─────────┘
              │ YES                         │ NO
              ▼                             ▼
    ┌──────────────────────┐    ┌──────────────────────────┐
    │  PATH 1: NESTED      │    │  DECISION 2: Has         │
    │  CUSTOM LEDGER       │    │  hierarchy fields?       │
    └──────┬───────────────┘    └────┬────────────────┬────┘
           │                         │ YES            │ NO
           ▼                         ▼                ▼
    ┌──────────────────┐    ┌─────────────────┐  ┌──────────────┐
    │ Get parent code  │    │  PATH 2:        │  │  PATH 3:     │
    │ Example: "9001"  │    │  HIERARCHY      │  │  FALLBACK    │
    └──────┬───────────┘    │  BASED          │  └──────┬───────┘
           │                └────┬────────────┘         │
           ▼                     ▼                      ▼
    ┌──────────────────┐    ┌─────────────────┐  ┌──────────────┐
    │ Find max suffix  │    │ Lookup code     │  │ Find max in  │
    │ under parent     │    │ from master_    │  │ 9000 range   │
    │                  │    │ hierarchy_raw   │  │              │
    └──────┬───────────┘    └────┬────────────┘  └──────┬───────┘
           │                     │                      │
           ▼                     ▼                      ▼
    ┌──────────────────┐    ┌─────────────────┐  ┌──────────────┐
    │ Increment suffix │    │ Truncate to     │  │ Increment    │
    │ .001 → .002      │    │ appropriate     │  │ 9001 → 9002  │
    │                  │    │ level length    │  │              │
    └──────┬───────────┘    └────┬────────────┘  └──────┬───────┘
           │                     │                      │
           ▼                     ▼                      │
    ┌──────────────────┐    ┌─────────────────┐        │
    │ Result:          │    │ Find max suffix │        │
    │ "9001.001"       │    │ at this level   │        │
    │ "9001.002"       │    └────┬────────────┘        │
    └──────┬───────────┘         │                     │
           │                     ▼                     │
           │              ┌─────────────────┐          │
           │              │ Append suffix   │          │
           │              │ .001, .002, etc │          │
           │              └────┬────────────┘          │
           │                   │                       │
           │                   ▼                       │
           │              ┌─────────────────┐          │
           │              │ Result:         │          │
           │              │ "01010101.001"  │          │
           │              │ "01010101.002"  │          │
           │              └────┬────────────┘          │
           │                   │                       │
           └───────────────────┴───────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  VALIDATE UNIQUENESS │
                    │  (within tenant)     │
                    └──────────┬───────────┘
                               │
                    ┌──────────┴──────────┐
                    │ Unique?             │
                    └──┬──────────────┬───┘
                  YES  │              │ NO (race condition)
                       ▼              ▼
              ┌────────────────┐  ┌──────────────┐
              │ SAVE LEDGER    │  │ RETRY (max 3 │
              │ with code      │  │ attempts)    │
              └────────┬───────┘  └──────┬───────┘
                       │                 │
                       ▼                 │
              ┌────────────────┐         │
              │ ✅ SUCCESS     │◄────────┘
              │ Return ledger  │
              │ with code      │
              └────────────────┘
```

## 🎯 Code Format Examples

### Path 1: Nested Custom Ledger
```
Parent Ledger: "Main Account" (code: 9001)
  └─ Child 1: "Branch A"        → 9001.001
  └─ Child 2: "Branch B"        → 9001.002
      └─ Grandchild: "Cash"     → 9001.002.001
```

### Path 2: Hierarchy-Based
```
master_hierarchy_raw:
  major_group_1: "Assets"
  group_1: "Current Assets"
  sub_group_1_1: "Cash & Bank"
  code: "010101010101"

Truncate to group level (8 digits): "01010101"
  └─ Custom 1: "Petty Cash"     → 01010101.001
  └─ Custom 2: "Main Cash"      → 01010101.002
  └─ Custom 3: "Branch Cash"    → 01010101.003
```

### Path 3: Fallback
```
No hierarchy or parent specified:
  └─ Ledger 1: "Custom Account 1"  → 9001
  └─ Ledger 2: "Custom Account 2"  → 9002
  └─ Ledger 3: "Custom Account 3"  → 9003
```

## 🔢 Code Length Reference

| Hierarchy Level | Code Length | Example | Source |
|----------------|-------------|---------|--------|
| Category (Major Group) | 6 digits | `010101` | master_hierarchy_raw |
| Group | 8 digits | `01010101` | master_hierarchy_raw |
| Sub Group 1 | 10 digits | `0101010101` | master_hierarchy_raw |
| Sub Group 2 | 12 digits | `010101010101` | master_hierarchy_raw |
| Sub Group 3 | 14 digits | `01010101010101` | master_hierarchy_raw |
| Ledger Type | 16 digits | `0101010101010101` | master_hierarchy_raw |
| **Custom Suffix** | **+4 chars** | **`.001`** | **Auto-generated** |

## 🔄 Retry Logic for Race Conditions

```
Attempt 1: Generate code "01010101.001"
           ↓
           Check uniqueness
           ↓
           ❌ Already exists (another request created it)
           ↓
Attempt 2: Re-generate code "01010101.002"
           ↓
           Check uniqueness
           ↓
           ✅ Unique! Save ledger
```

## 🎨 Visual Code Structure

```
┌─────────────────────────────────────────────────────────┐
│                    LEDGER CODE                          │
├─────────────────────────────────┬───────────────────────┤
│      Hierarchy Prefix           │   Unique Suffix       │
│   (from master_hierarchy_raw)   │   (auto-generated)    │
├─────────────────────────────────┼───────────────────────┤
│         01010101                │       .001            │
│         ↑                       │        ↑              │
│         │                       │        │              │
│    Identifies the               │   Distinguishes       │
│    group/category               │   individual ledgers  │
│    in hierarchy                 │   within same group   │
└─────────────────────────────────┴───────────────────────┘
```

## 📊 Database Interaction Flow

```
┌──────────────────┐
│  User Request    │
│  Create Ledger   │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│  Backend API (views.py)                      │
│  ┌────────────────────────────────────────┐  │
│  │ 1. Validate input data                 │  │
│  │ 2. Call generate_ledger_code()         │  │
│  │ 3. Retry logic (max 3 attempts)        │  │
│  │ 4. Save with transaction               │  │
│  └────────────────────────────────────────┘  │
└────────┬─────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│  Code Generation (utils.py)                  │
│  ┌────────────────────────────────────────┐  │
│  │ Query master_hierarchy_raw             │◄─┼─── Read-only
│  │ SELECT code FROM master_hierarchy_raw  │  │     global data
│  │ WHERE group_1 = 'Current Assets'       │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │ Query existing ledgers                 │◄─┼─── Check for
│  │ SELECT code FROM master_ledgers        │  │     existing codes
│  │ WHERE code LIKE '01010101.%'           │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │ Calculate next suffix                  │  │
│  │ max_suffix + 1 → .001, .002, etc       │  │
│  └────────────────────────────────────────┘  │
└────────┬─────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│  Database (master_ledgers table)             │
│  ┌────────────────────────────────────────┐  │
│  │ INSERT INTO master_ledgers             │  │
│  │ (name, code, category, group, ...)     │  │
│  │ VALUES ('Petty Cash', '01010101.001',  │  │
│  │         'Assets', 'Current Assets')    │  │
│  └────────────────────────────────────────┘  │
└────────┬─────────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│  Response to     │
│  Frontend        │
│  {               │
│    "id": 123,    │
│    "code":       │
│    "01010101.001"│
│  }               │
└──────────────────┘
```

## 🧪 Test Scenarios Visualization

```
Test 1: Sequential Creation
─────────────────────────────
Request 1 → "Cash"     → 01010101.001 ✅
Request 2 → "Bank"     → 01010101.002 ✅
Request 3 → "Savings"  → 01010101.003 ✅


Test 2: Nested Ledgers
──────────────────────
Parent: "Main" (9001)
  ├─ Child 1 → 9001.001 ✅
  ├─ Child 2 → 9001.002 ✅
  └─ Child 3 → 9001.003 ✅
      └─ Grandchild → 9001.003.001 ✅


Test 3: Concurrent Creation (Race Condition)
─────────────────────────────────────────────
Time  │ Request A        │ Request B
──────┼──────────────────┼──────────────────
T1    │ Generate .001    │ Generate .001
T2    │ Save .001 ✅     │ Save .001 ❌ (collision)
T3    │ Return success   │ Retry...
T4    │                  │ Generate .002
T5    │                  │ Save .002 ✅
T6    │                  │ Return success


Test 4: Missing Hierarchy Data
───────────────────────────────
Request: Create ledger with group "XYZ"
  ↓
Lookup in master_hierarchy_raw
  ↓
Not found ❌
  ↓
Fallback to 9000 range
  ↓
Result: 9001 ✅
```

## 🔐 Security Considerations

```
❌ VULNERABLE (Old approach):
   query = f"SELECT code FROM master_hierarchy_raw WHERE group_1 = '{value}'"
   # SQL injection risk!

✅ SECURE (Current approach):
   cursor.execute(
       "SELECT code FROM master_hierarchy_raw WHERE group_1 = %s",
       [value]
   )
   # Parameterized query - safe!
```

## 📈 Performance Optimization

```
┌─────────────────────────────────────────────┐
│  Optimization Strategy                      │
├─────────────────────────────────────────────┤
│  1. Index on master_hierarchy_raw.code      │
│  2. Index on master_ledgers.code            │
│  3. Index on master_ledgers.tenant_id       │
│  4. Use SELECT ... LIMIT 1 for lookups      │
│  5. Transaction isolation for uniqueness    │
└─────────────────────────────────────────────┘

Expected Performance:
  Code Generation: < 100ms
  Database Lookup: < 50ms
  Total Creation: < 200ms
```

---

**This diagram is part of the Ledger Code Assignment Implementation Plan**
See: `.agent/implementation_plans/ledger_code_assignment.md`
