# MySQL Configuration - Increased max_connections to 200
# Add this to your MySQL configuration file (my.ini or my.cnf)

[mysqld]
max_connections = 200

# Other recommended settings for production
max_allowed_packet = 64M
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M
